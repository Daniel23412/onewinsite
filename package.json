{
  "name": "pixage-portal",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": {
    "start": "node --env-file-if-exists=.env scripts/serve.mjs",
    "dev": "node --env-file-if-exists=.env --watch scripts/serve.mjs",
    "test": "node --test tests/*.test.mjs",
    "build": "node scripts/validate.mjs"
  }
}
