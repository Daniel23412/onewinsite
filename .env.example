# Server settings only. Keep real values out of GitHub and public/.
BOT_TOKEN=
POSTBACK_LOG_CHAT_ID=
POSTBACK_SECRET=
# Optional: overrides public/site-config.json.
AFFILIATE_URL=
# Optional separate click-signing key; defaults to POSTBACK_SECRET.
CLICK_SIGNING_SECRET=
# Optional durable duplicate protection (both required).
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
PORT=3000
